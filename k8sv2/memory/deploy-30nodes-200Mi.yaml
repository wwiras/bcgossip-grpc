apiVersion: apps/v1
kind: Deployment
metadata:
  name: bcgossip30nodes200mi
spec:
  selector:
    matchLabels:
      run: bcgossip
  replicas: 30
  template:
    metadata:
      labels:
        run: bcgossip
    spec:
      containers:
      - name: bcgossip-cont
        image: wwiras/bcgossip-grpc:v15  # Replace with your actual Docker image
        ports:
        - containerPort: 5050
        resources:
          requests:
            memory: 180Mi
          limits:
            memory: 200Mi

