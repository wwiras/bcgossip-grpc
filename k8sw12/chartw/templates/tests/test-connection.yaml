apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chartw.fullname" . }}-test-connection"
  labels:
    {{- include "chartw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chartw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
